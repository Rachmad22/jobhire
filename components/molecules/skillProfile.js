import React from "react";
import style from "../../styles/pages/profile.module.scss"

export default function Skill() {

    return (

        <>
            <span className={`badge bg-warning ${style.skillBadge}`}>Phyton</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>Laravel</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>Golang</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>Javascript</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>PHP</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>HTML</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>C++</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>Kotlin</span>
            <span className={`badge bg-warning ${style.skillBadge}`}>Swift</span>

        </>

    )
}
